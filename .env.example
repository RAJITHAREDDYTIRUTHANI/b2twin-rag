# Example environment variables for B2Twin RAG setup

# API keys (add your own locally, do NOT commit them)
ANTHROPIC_API_KEY=your_api_key_here

# Flask app settings
FLASK_ENV=development
PORT=5000

# Optional: database URL or Redis URL if used
DATABASE_URL=your_database_url_here
REDIS_URL=your_redis_url_here
